[project]
name = "simple-filter"
version = "0.1.0"
description = "Filter"
requires-python = ">=3.12"
dependencies = [
    "kip[cad]",
    "numpy>=2.5.3",
    "pymupdf>=1.28.2",
    "pyvista>=0.49.0",
]

[tool.uv]
package = false

[tool.uv.sources]
kip = { path = "../" }
